apiVersion: v1
kind: Service
metadata:
  name: judica-backend
  namespace: judica
  labels:
    app: judica-backend
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: judica-backend
