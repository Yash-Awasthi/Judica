apiVersion: v1
kind: Service
metadata:
  name: judica-frontend
  namespace: judica
  labels:
    app: judica-frontend
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - port: 4173
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: judica-frontend
