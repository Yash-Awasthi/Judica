-- CreateTable
CREATE TABLE "Evaluation" (
    "id" SERIAL NOT NULL,
    "sessionId" TEXT NOT NULL,
    "conversationId" TEXT NOT NULL,
    "userId" INTEGER NOT NULL,
    "coherence" DOUBLE PRECISION NOT NULL DEFAULT 0.0,
    "consensus" DOUBLE PRECISION NOT NULL DEFAULT 0.0,
    "diversity" DOUBLE PRECISION NOT NULL DEFAULT 0.0,
    "quality" DOUBLE PRECISION NOT NULL DEFAULT 0.0,
    "efficiency" DOUBLE PRECISION NOT NULL DEFAULT 0.0,
    "overallScore" DOUBLE PRECISION NOT NULL DEFAULT 0.0,
    "recommendations" JSONB,
    "strengths" JSONB,
    "weaknesses" JSONB,
    "timestamp" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,

    CONSTRAINT "Evaluation_pkey" PRIMARY KEY ("id")
);

-- AddIndex
CREATE INDEX "Evaluation_userId_timestamp_idx" ON "Evaluation"("userId", "timestamp");

-- AddIndex
CREATE INDEX "Evaluation_sessionId_idx" ON "Evaluation"("sessionId");

-- AddForeignKey
ALTER TABLE "Evaluation" ADD CONSTRAINT "Evaluation_userId_fkey" FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE;

-- AddColumn
ALTER TABLE "AuditLog" ADD COLUMN "metadata" JSONB;
